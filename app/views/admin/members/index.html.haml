%h1 Members Management

%p= link_to "Add new member", new_admin_member_path, class: "btn"

%table
  %colgroup
    %col{style: "width: 80%"}
    %col{style: "width: 20%"}
  %thead
    %tr
      %th= sortable_heading_for(:email)
      %th.actions Actions
  %tbody
    - @users.each do |user|
      %tr
        %td= user.email
        %td.actions
          - if policy(user).edit?
            = link_to "Edit", edit_admin_member_path(user), class: "btn btn-small"
          - if policy(user).destroy?
            = link_to "Delete", admin_member_path(user), method: :delete, data: { confirm: "Are you sure you want to delete #{user}?" }, class: "btn btn-small"

= paginate @users
