= simple_form_for @site, url: admin_sites_path do |f|
  = f.input :name
  = f.input :rural, as: :boolean

  = f.simple_fields_for :address do |ff|
    = ff.input :line_1
    = ff.input :post_code
    = ff.association :state, collection: State.all

  %p= f.submit