<h1>Editing site</h1>

<%= form_for :site, url: admin_site_path(@site), method: :patch do |f| %>

  <% if @site.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@site.errors.count, "error") %> prohibited
        this site from being saved:
      </h2>
      <ul>
        <% @site.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :rural %>
    <%= f.check_box :rural %>

  </p>

  <p></p>

  <p>
    <%= f.submit %>
  </p>

<% end %>

<%= link_to 'Back', admin_sites_path %>