.authentication

  %h2= current_page_title

  = simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "authentication-form", method: :put }) do |f|

    = f.input :given_names, autofocus: true, input_html: {maxlength: User::MAX_NAME_LENGTH}
    = f.input :family_name, input_html: {maxlength: User::MAX_NAME_LENGTH}
    = f.input :email
    - if devise_mapping.confirmable? && resource.pending_reconfirmation?
      %p Currently waiting confirmation for: #{resource.unconfirmed_email}
    = f.input :password, autocomplete: "off", hint: "Minimum is #{Rails.configuration.devise.password_length.min} characters. Leave blank if you don't want to change it."
    = f.input :password_confirmation, autocomplete: "off"
    = f.input :current_password, autocomplete: "off", hint: "Required to confirm your changes"
    = f.button :submit, "Update"
