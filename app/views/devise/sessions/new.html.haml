%h2 Sign in
= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
  %div
    = f.label :email
    = f.email_field :email, autofocus: true
  %div
    = f.label :password
    = f.password_field :password, autocomplete: "off"
  - if devise_mapping.rememberable?
    %div
      = f.check_box :remember_me
      = f.label :remember_me
  %div= f.submit "Sign in"
= render "devise/shared/links"

- if Rails.application.config.should_show_easy_login
  .easy-sign-in
    %h3 Easy Sign-in
    %ul
      %li= link_to 'Sign in as Administrator', '#', data: { email: "admin@example.com", password: "password" }
      %li= link_to 'Sign in as Member', '#', data: { email: "member@example.com", password: "password" }
