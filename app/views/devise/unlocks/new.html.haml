.authentication

  %h2 Resend unlock instructions

  = simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { class: "authentication-form", method: :post }) do |f|

    = f.input :email, autofocus: true
    = f.button :submit, "Resend unlock instructions"

  = render "devise/shared/links"
